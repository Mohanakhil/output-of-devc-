#include<stdio.h>
#include<string.h>
main()
{
	int i,j=0,L1,L2;
	char a[100],b[100];
	printf("enter first string\n");
	gets(a);
	printf("enter second string\n");
	gets(b);
	L1=strlen(a);L2=strlen(b);
	if(L1!=L2)
{
		printf("strings are not equal\n");
	if(a[i]>b[i])
	printf("string1 is greater than string2");
	else printf("string2 is greater than string1");
	}while(i<L1)
	{if(a[i]==b[i]){
	
	i++;printf("i=%d\n",i);}
	else
	 j++;printf("j=%d\n",j);
	}
	if(i==L1) printf("strings are equal");
	else 
	if(j>0)
	printf("strings are equal but not same");
}
